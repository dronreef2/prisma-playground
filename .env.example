# Variáveis de Ambiente para Desenvolvimento Local
# Copie este arquivo para .env.local e configure suas variáveis

# URL do banco de dados (para desenvolvimento local com Prisma)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prisma_playground"

# URL direta do banco (mesmo valor que DATABASE_URL para desenvolvimento local)  
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/prisma_playground"

# Configurações Supabase (opcionais - para conectar a um banco real)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Ambiente
NODE_ENV="development"
